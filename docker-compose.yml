services:
  api:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - streamsense-data:/data
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

volumes:
  streamsense-data:

